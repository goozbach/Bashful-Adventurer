# some fun functions

## mkcd
create and enter a directory

    # mkdir, cd into it
    mkcd () {
        mkdir -p -- "$@" && cd -- "$@"
    }

* live demo with alice *

## pathmunge
Redhatism -- Add the dir passed to the function to the front or back of the PATH

    pathmunge () {
      if ! echo $PATH | grep -E -q "(^|:)$1($|:)" ; then
       if [ "$2" = "after" ] ; then
          PATH=$PATH:$1
       else
          PATH=$1:$PATH
       fi
      fi
    }
